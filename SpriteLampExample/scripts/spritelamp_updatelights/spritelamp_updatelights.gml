
if(u_numLights >= 1)
    lightSource1 = instance_nth_nearest(x,y,oLightSource,1);
if(u_numLights >= 2)
    lightSource2 = instance_nth_nearest(x,y,oLightSource,2);
if(u_numLights >= 3)
    lightSource3 = instance_nth_nearest(x,y,oLightSource,3);
if(u_numLights >= 4)
    lightSource4 = instance_nth_nearest(x,y,oLightSource,4);
    